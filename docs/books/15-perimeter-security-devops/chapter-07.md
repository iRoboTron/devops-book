# Глава 7: IDS/IPS и сетевые сигналы

> **Запомни:** IDS/IPS полезны не тогда, когда ты их поставил, а тогда, когда понимаешь, какие сигналы тебе действительно нужны и что делать после срабатывания.

---

## 7.1 IDS и IPS — в чём разница

### IDS

Intrusion Detection System:
- наблюдает;
- сигнализирует;
- сама не обязана блокировать.

### IPS

Intrusion Prevention System:
- стоит inline или логически близко к потоку;
- может не только увидеть, но и заблокировать.

На практике small teams часто начинают с detection, а не с prevention.

---

## 7.2 Примеры инструментов

| Инструмент | Что делает | Когда нужен |
|---|---|---|
| Suricata | Сигнатурный IDS/IPS, глубокий анализ пакетов | Small business и выше, если есть кому разбирать алерты |
| Snort | Классический IDS/IPS с большим набором правил | Учебные стенды и legacy среды |
| Zeek | Сетевая телеметрия и аналитика, а не просто бан по сигнатуре | Forensics, разбор инцидентов, длинный контекст по трафику |

Как выбирать:
- `Suricata` подходит, когда нужен современный сигнатурный движок и, возможно, IPS-режим;
- `Snort` полезно знать как базовый классический инструмент, но для нового стенда чаще смотрят на Suricata;
- `Zeek` не блокирует трафик сам по себе, зато хорошо отвечает на вопрос "что реально происходило в сети".

Практический вывод: на одном VPS без процесса triage Zeek и Suricata легко создадут больше шума, чем пользы.

---

## 7.3 Когда это оправдано

### Один VPS

Обычно не стоит сразу тащить тяжёлый IDS/IPS, если:
- у тебя один сервис;
- нет ресурсов на разбор срабатываний;
- нет даже нормального firewall и логов.

Сначала:
- сократи поверхность атаки;
- включи SSH hardening;
- настрой firewall;
- разберись с proxy-логами;
- потом думай про дополнительные сигналы.

### Small business

Уже полезно:
- видеть сканирование и шумный трафик;
- иметь точку сетевого наблюдения;
- различать нормальный и подозрительный traffic pattern.

### Enterprise

IDS/IPS часто становится нормальной частью сети, но только вместе с процессами triage.

---

## 7.4 Практика: что можно сделать уже сейчас

Даже без полноценной IPS ты можешь:

- фиксировать входящие сканы в логах и pcap;
- коррелировать странные bursts с firewall hits;
- понимать, какой трафик идёт к тебе на самом деле.

Минимум практики:

```bash
sudo tcpdump -ni any port 22
sudo tcpdump -ni any port 80
sudo tcpdump -ni any host YOUR_TEST_CLIENT
```

Смысл здесь — увидеть реальный traffic pattern, а не "поставить галочку IDS".

Если хочешь увидеть именно признак port scan, используй более узкий фильтр:

```bash
sudo tcpdump -ni eth0 'tcp[tcpflags] & (tcp-syn) != 0' -c 20
```

Пример вывода при `nmap`-скане:

```
03:15:01 IP 10.0.0.5.44321 > 10.0.0.10.22: Flags [S], seq 123456, win 64240
03:15:01 IP 10.0.0.5.44322 > 10.0.0.10.80: Flags [S], seq 123457, win 64240
03:15:01 IP 10.0.0.5.44323 > 10.0.0.10.443: Flags [S], seq 123458, win 64240
03:15:01 IP 10.0.0.5.44324 > 10.0.0.10.3306: Flags [S], seq 123459, win 64240
```

Что искать:
- один IP за доли секунды стучится в несколько разных портов;
- флаг `S` означает SYN, то есть попытку начать TCP-соединение;
- именно такой рисунок IDS-движок интерпретирует как сканирование или reconnaissance.

---

## 7.5 Lab-only проверка

На своей lab-машине:

```bash
nmap -Pn SERVER_IP
```

Параллельно на сервере:

```bash
sudo tcpdump -ni any host CLIENT_IP
```

Ты должен руками увидеть, как выглядит сетевой шум от controlled scan.

Если в lab есть Suricata/Zeek, можно сравнить:
- что видит сырая сеть;
- что видит IDS-движок;
- что полезно защитнику, а что только создаёт шум.

---

## 7.6 Что опасно в IDS/IPS

- ложные срабатывания;
- желание блокировать всё подряд;
- отсутствие triage-процесса;
- тяжёлая система раньше, чем настроен базовый defensive hygiene.

Инженерный вопрос всегда такой: **кто будет разбирать эти сигналы и что делать после них?**

---

## 7.7 Чеклист главы

- [ ] Я понимаю разницу между IDS и IPS
- [ ] Я знаю, когда IDS/IPS оправдана, а когда это premature complexity
- [ ] Я умею увидеть controlled scan в `tcpdump`
- [ ] Я понимаю, что сетевые сигналы полезны только вместе с процессом разбора
