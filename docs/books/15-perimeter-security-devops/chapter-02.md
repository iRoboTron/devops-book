# Глава 2: Базовый Linux hardening

> **Запомни:** лучший открытый порт — тот, которого вообще нет. Лучший лишний сервис — тот, который не установлен и не запущен.

---

## 2.1 Что значит hardening хоста

Hardening Linux-хоста — это не одна команда и не "магический скрипт". Это дисциплина:

- убрать лишние сервисы;
- не слушать наружу там, где можно слушать только localhost;
- минимизировать пользователей и права;
- понимать, что запущено и почему.

---

## 2.2 Быстрый аудит хоста

Начни с инвентаризации.

### Какие сервисы слушают порты

```bash
ss -tulpn
sudo lsof -i -P -n | grep LISTEN
```

### Какие службы активны

```bash
systemctl list-units --type=service --state=running
```

### Какие пользователи есть в системе

```bash
cut -d: -f1 /etc/passwd
```

### Какие процессы сетевые и долгоживущие

```bash
ps -ef
sudo pstree -p
```

---

## 2.3 На что смотреть особенно внимательно

### Сервисы, которые часто открывают лишнее

- PostgreSQL `:5432`
- MySQL/MariaDB `:3306`
- Redis `:6379`
- Admin panels `:8080`, `:9000`, `:9090`
- Docker API если кто-то открыл его бездумно

Пример вывода `ss -tulpn`, где уже видно проблему:

```
tcp LISTEN 0.0.0.0:6379   users:(("redis-server",pid=923,fd=6))
tcp LISTEN 0.0.0.0:9090   users:(("grafana-server",pid=1047,fd=8))
tcp LISTEN 127.0.0.1:8000 users:(("python3",pid=1550,fd=7))
```

Как это читать:
- `0.0.0.0:6379` — Redis открыт всем интерфейсам, хотя чаще всего он нужен только приложению;
- `0.0.0.0:9090` — dashboard торчит наружу, это лишняя публичная поверхность;
- `127.0.0.1:8000` — сервис доступен только локально, это безопаснее для backend за proxy.

### Принцип

Если сервис нужен только локально, он должен слушать:

```text
127.0.0.1
```

а не:

```text
0.0.0.0
```

---

## 2.4 Пользователи и права

Минимум:
- отдельный обычный пользователь для администрирования;
- не работать под `root` постоянно;
- отключить ненужные пользователи приложений, если они не нужны;
- проверить sudo-права.

### Проверка sudo

```bash
sudo -l
getent group sudo
```

Нужно понимать, кто именно может:
- логиниться;
- использовать `sudo`;
- менять конфиги;
- читать чувствительные файлы.

---

## 2.5 Практика: уменьшить поверхность атаки на хосте

### Шаг 1. Остановить и отключить лишнее

```bash
sudo systemctl stop SERVICE
sudo systemctl disable SERVICE
```

### Шаг 2. Проверить, что сервис больше не слушает порт

```bash
ss -tulpn | grep PORT
```

### Шаг 3. Если сервис нужен только локально — перенастроить bind address

Примеры:

- PostgreSQL: `listen_addresses = 'localhost'`
- admin UI: слушать только `127.0.0.1`
- backend behind proxy: bind на `127.0.0.1:8000`

Практический пример для PostgreSQL:

```bash
sudo nano /etc/postgresql/*/main/postgresql.conf
```

Внутри файла:

```ini
listen_addresses = 'localhost'
```

Применение:

```bash
sudo systemctl restart postgresql
ss -tulpn | grep 5432
```

Ожидаемый вывод после:

```
tcp LISTEN 127.0.0.1:5432 users:(("postgres",pid=1044,fd=5))
```

Почему это важно: теперь PostgreSQL принимает подключения только с самого хоста, и даже ошибка во внешнем firewall не сделает его публичным.

### Шаг 4. Снова снять baseline

```bash
ss -tulpn
systemctl list-units --type=service --state=running
```

---

## 2.6 Lab-only проверка

На своей VM сделай controlled check:

1. подними лишний тестовый сервис на `0.0.0.0:9000`;
2. проверь, что он виден с другой своей машины;
3. переведи его на `127.0.0.1` или выключи;
4. проверь снова.

Так ты руками увидишь, как одна строка bind address меняет поверхность атаки.

---

## 2.7 Типовые ошибки

- удалять сервисы без baseline и потом не понимать, что сломалось;
- считать, что если сервис закрыт firewall-ом, то можно не менять bind address;
- держать админки и БД на `0.0.0.0` "потому что так проще";
- не проверять пользователей и sudo-права после быстрого bootstrap.

---

## 2.8 Чеклист главы

- [ ] Я знаю, какие сервисы слушают порты на моём хосте
- [ ] Я могу быстро найти лишние сетевые сервисы
- [ ] Я понимаю разницу между `127.0.0.1` и `0.0.0.0`
- [ ] Я проверил пользователей и sudo-права
- [ ] Я уменьшил поверхность атаки хотя бы на одном своём стенде
